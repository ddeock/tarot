{
  "name": "shopimg-mall-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root configuration to auto-deploy the server folder on Cloudtype",
  "scripts": {
    "postinstall": "cd server && npm install",
    "start": "cd server && npm start"
  }
}
